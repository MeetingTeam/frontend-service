import env from "react-dotenv";

// API Envs
export const {
          USER_SERVICE_ENDPOINT,
          TEAM_SERVICE_ENDPOINT,
          CHAT_SERVICE_ENDPOINT,
          MEETING_SERVICE_ENDPOINT,
          WEBSOCKET_SERVICE_ENDPOINT
} = env;
